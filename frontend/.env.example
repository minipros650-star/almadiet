[TEMPLATE]
# AlmaDiet — PUBLIC Flutter configuration (template).
#
# Copy to env.local.json for local development and inject with:
#   flutter run --dart-define-from-file=env.local.json
#
# NEVER add these files with real values to version control.
# NEVER put SUPABASE_SECRET_KEY (or legacy SUPABASE_SERVICE_ROLE_KEY),
# DATABASE_URL, JWT secrets, or Google client secrets here — Flutter
# receives PUBLIC values only.
{
  "SUPABASE_URL": "https://YOUR-PROJECT-REF.supabase.co",
  "SUPABASE_PUBLISHABLE_KEY": "sb_publishable_YOUR-PUBLIC-KEY",
  "API_BASE_URL": "https://YOUR-PROJECT.vercel.app"
}
